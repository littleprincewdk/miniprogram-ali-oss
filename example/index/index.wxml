<button type="primary" bindtap="upload">Upload</button>
<view style="height: 100rpx" />
<image style="width: 100%" mode="aspectFill" src="{{filepath}}" />
<view style="height: 100rpx" />
<progress wx:if="{{progress}}" percent="{{progress}}" />
